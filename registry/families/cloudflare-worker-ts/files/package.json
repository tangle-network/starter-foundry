{
  "name": "{{packageName}}",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node --experimental-strip-types validate-worker.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
