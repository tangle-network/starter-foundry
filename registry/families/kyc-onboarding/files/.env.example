# KYC provider selection. Values: 'sandbox' (local mock) or 'persona', 'sumsub', etc.
# The UI reads this via import.meta.env.VITE_KYC_PROVIDER.
VITE_KYC_PROVIDER=sandbox

# Provider API key — inject at runtime, never commit real values.
# VITE_KYC_API_KEY=your-provider-key
