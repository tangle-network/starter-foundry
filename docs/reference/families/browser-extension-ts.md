# Family: `browser-extension-ts`

Manifest v3 browser extension TypeScript starter with popup preview.

**Taxonomy**: language=typescript · runtime=browser-extension · surface=extension

**Tags**: browser-extension, chrome, manifest-v3, typescript

## Slots

- `auth` — options: auth:none, auth:better-auth, auth:clerk, auth:supabase-auth (default: `auth:none`)
- `sdk` — options: sdk:none, sdk:coinbase-cdp, sdk:solana-web3, sdk:evm-wallet (default: `sdk:none`)

## Routing keywords

- **tier2**: browser extension, chrome extension, manifest v3, extension popup, firefox addon
