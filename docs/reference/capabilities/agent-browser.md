# Capability: `capability:agent-browser`

Adds browser automation agent patterns with DOM interaction, screenshots, and action replay.

**Applies to**: agent-service-ts, agent-service-py, playwright-worker
