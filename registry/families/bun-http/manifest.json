{
  "id": "bun-http",
  "description": "Bun HTTP API starter using Bun.serve() \u2014 zero-transpile TS, native Bun runtime, instant cold start.",
  "tags": [
    "bun",
    "api",
    "backend",
    "typescript"
  ],
  "taxonomy": {
    "language": "typescript",
    "runtime": "bun",
    "surface": "api"
  },
  "slots": {
    "database": {
      "options": [
        "database:sqlite",
        "database:postgres",
        "database:mongodb",
        "database:convex"
      ],
      "default": "database:sqlite"
    },
    "auth": {
      "options": [
        "auth:none",
        "auth:better-auth",
        "auth:clerk"
      ],
      "default": "auth:none"
    },
    "payments": {
      "options": [
        "payments:none",
        "payments:stripe"
      ],
      "default": "payments:none"
    },
    "queue": {
      "options": [
        "queue:none",
        "queue:bullmq",
        "queue:trigger-dev"
      ],
      "default": "queue:none"
    }
  },
  "defaults": {
    "projectType": "api",
    "serviceName": "starter-foundry-bun",
    "port": 8095,
    "databaseProvider": "sqlite"
  },
  "files": [
    {
      "source": "files/package.json",
      "target": "package.json"
    },
    {
      "source": "files/tsconfig.json",
      "target": "tsconfig.json"
    },
    {
      "source": "files/bunfig.toml",
      "target": "bunfig.toml"
    },
    {
      "source": "files/validate-bun.mjs",
      "target": "validate-bun.mjs"
    }
  ],
  "validationChecks": [
    {
      "type": "file-exists",
      "path": "src/server.ts"
    },
    {
      "type": "file-exists",
      "path": "package.json"
    },
    {
      "type": "command-success",
      "command": [
        "node",
        "validate-bun.mjs"
      ],
      "expect": "bun starter ok"
    }
  ],
  "contextHints": {
    "commands": [
      "bun install",
      "bun run dev",
      "bun test"
    ],
    "entrypoints": [
      "src/server.ts",
      "src/handlers.ts",
      "package.json"
    ],
    "preview": {
      "path": "/health"
    },
    "extensionPoints": [
      "database-config.json",
      "auth-config.json",
      "payments-config.json",
      "queue-config.json"
    ]
  },
  "keywords": [
    "bun",
    "bun.js",
    "bunjs",
    "bun runtime",
    "bun http",
    "bun.serve",
    "bun api"
  ],
  "scoring": {
    "boost": {
      "bun": 3
    }
  },
  "tieredKeywords": {
    "tier1": [
      "bun",
      "bun.serve",
      "bun runtime"
    ],
    "tier2": [
      "bun api",
      "bun http",
      "bun.js"
    ]
  },
  "buildHints": {
    "whenToUse": "Bun-native HTTP server. Measurably faster cold-start + throughput than Node for small APIs.",
    "firstSteps": [
      "Install deps with `bun install` \u2014 NOT `pnpm install`. Bun has its own lockfile (bun.lockb) and resolution.",
      "Run the dev server with `bun run dev` (hot-reload via --watch). Don't use nodemon / tsx / ts-node \u2014 Bun runs TypeScript directly.",
      "Tests use Bun's built-in runner: `bun test`. Do NOT add vitest/jest \u2014 Bun's runner is Jest-API-compatible and ships zero-config.",
      "Extend `src/handlers.ts` \u2014 add new routes to the `routes` map. Handlers are Request \u2192 Response; Bun.serve invokes them per request.",
      "Env vars: `process.env` OR `Bun.env` (both work). `.env` files auto-load under `bun run`."
    ],
    "gotchas": [
      "Do NOT run `pnpm add` / `npm install` to add deps. Use `bun add <pkg>`. Mixing package managers corrupts node_modules symlink resolution.",
      "TypeScript types are auto-stripped \u2014 no tsc step. If CI needs real type-checking, add `bun tsc --noEmit` explicitly.",
      "Bun's Node compat layer is not 100% \u2014 cluster/worker_threads/fs-native APIs that wrap libuv primitives may differ."
    ],
    "architectureNotes": [
      "Bun.serve returns a Server with `port` and `hostname` \u2014 bind it at module scope; don't call it from within a handler.",
      "Request/Response are standard Fetch API \u2014 handlers are portable to Cloudflare Workers, Deno, fetch-based edge runtimes."
    ],
    "placeholders": [
      {
        "path": "src/handlers.ts",
        "description": "Default routes map with just `/health`. Add the product's endpoints here \u2014 each route is `(Request) => Response | Promise<Response>`."
      }
    ]
  }
}
