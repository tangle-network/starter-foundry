# Family: `python-worker`

Python background worker starter for async tasks, cron jobs, and queue processing.

**Taxonomy**: language=python · runtime=python · surface=worker

**Tags**: python, worker, jobs, async

## Slots

- `queue` — options: queue:none, queue:bullmq, queue:trigger-dev (default: `queue:none`)
- `database` — options: database:sqlite, database:postgres, database:mongodb (default: `database:sqlite`)

## Routing keywords

- **tier1**: python worker, python cron, python queue, python background job, celery, python task, python async worker, python celery, celery worker
