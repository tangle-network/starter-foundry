[package]
name = "guest"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "guest"
path = "src/main.rs"

[dependencies]
risc0-zkvm = { version = "2.1", default-features = false, features = ["std"] }
serde = { version = "1", features = ["derive"] }
