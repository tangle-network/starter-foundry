{
  "name": "{{packageName}}",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run deploy/deploy.ts"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@fhevm/solidity": "^0.11.1",
    "@fhevm/hardhat-plugin": "latest",
    "@fhevm-sdk/core": "^0.1.6"
  }
}
