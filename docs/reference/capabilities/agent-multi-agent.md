# Capability: `capability:agent-multi-agent`

In-process sub-agent tool-routing for single agent-service starters — the main agent delegates to specialist sub-agents exposed as tools. For full graph-based swarm orchestration, use family:agent-swarm-ts instead.

**Applies to**: agent-service-ts, agent-service-py, agent-service-rust
