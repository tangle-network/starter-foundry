# Ollama server base URL. Override to point at a remote or differently-mapped instance.
OLLAMA_BASE_URL=http://localhost:11434

# Model to use for inference. Must be pulled first:
#   docker compose exec ollama ollama pull llama3.2
OLLAMA_MODEL=llama3.2
