# {{headline}}

{{subheadline}}

This book is built with [Jupyter Book](https://jupyterbook.org/). Edit
`_toc.yml` to add chapters, and drop Markdown or `.ipynb` notebooks under
`content/`.

## Build

```
pip install -r requirements.txt
jupyter-book build .
```

The static site lands in `_build/html/`.
