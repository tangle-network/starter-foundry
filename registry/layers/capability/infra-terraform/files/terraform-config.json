{
  "tool": "terraform",
  "provider": "aws",
  "resources": ["compute", "database", "networking", "storage"],
  "stateBackend": "s3",
  "notes": ["Generate infra/main.tf with provider and resource blocks.", "Use variables.tf for environment-specific configuration.", "State backend should use S3+DynamoDB for team collaboration.", "Run terraform init, plan, apply workflow."]
}
