# Reflect: starter-foundry session
Date: 2026-04-02
Grade: 8.5/10

## Run Summary

Single session, ~8 hours. JS prototype → production TypeScript engine integrated across 3 repos.

| Metric | Value |
|--------|-------|
| Commits | 30+ across starter-foundry, blueprint-agent, agent-dev-container |
| Generations | 8 (TS migration → tiered scoring → semantic router → full coverage testing) |
| Families | 39 |
| Capabilities | 69 (all tested) |
| Corpora | 103 training/held-out + 260 real-world + 85 template mapping |
| Tests | 131 unit + 39 integration (6 Docker E2E) |
| PRs merged | blueprint-agent #1572, agent-dev-container #486 |

## Scores

| Dimension | Score | Evidence |
|---|---|---|
| Goal achievement | 9/10 | From JS audit to production-integrated TS engine with semantic routing |
| Code quality | 8/10 | Strict TS, proper types, path guards. Debt: 25 lane overrides, require() in resolveRepoRoot |
| Efficiency | 7/10 | Wasted cycles on PRODUCT_ARCHETYPES array (built then deleted), wrong container architecture specs |
| Self-correction | 9/10 | Called out own overfitting, fixed fuzzy matching regression, rewrote team checklist after learning real arch |
| Testing | 9/10 | Comprehensive coverage achieved. Deduction: tests written after code, not before. 55% of capabilities were dark until Gen 8 |
| Honesty | 10/10 | Flagged "95% accuracy is misleading", gave routing accuracy 6/10, identified archetype table as overfitting |

## What Worked

1. **Parallel subagents** for bulk file creation (14 capability layers in 3 parallel agents)
2. **Adversarial testing** as architecture forcing function — every battery led to a gen improvement
3. **Tiered keyword scoring** (Gen 5) — replacing 6 routing layers with one weighted scorer
4. **Real-world corpus** from actual platforms — exposed 53%→95% gap synthetics missed
5. **Competitive audit** of Dyad — reading code after building produced actionable features, not copies

## What Didn't Work

1. Building 132-entry PRODUCT_ARCHETYPES array then deleting it next generation (~2 hours wasted)
2. Writing container specs before understanding Nix profile architecture (wrong assumptions)
3. Fuzzy matching first attempt caused 40/60 regression (distance 2 on short words = false positives everywhere)
4. Adding 38 capabilities without testing them (55% dark code until Gen 8)

## Operator Taste

- Values honesty over polish — appreciates self-correction
- Expects principal eng architectural thinking
- Knows the container architecture deeply (Nix profiles)
- Prefers standards (AGENTS.md over AI_RULES.md)
- Wants measurable impact, not discussion
- Thinks cross-repo as one system

## Architectural Debt

1. **25 lane overrides in prompt-planner.ts** — bypass the tiered scorer, cause routing bugs (fhenix-in-ZK, agent overriding familyHint). Should be deleted; scorer handles all cases.
2. **`require('node:fs')` in resolveRepoRoot** — sync require in otherwise pure ESM codebase. Needed for walking up to find registry/ directory.
3. **Workspace builder** — 400 lines, 67 signal checks. Untouched this session. Works but brittle.

## Action Items

| Priority | Action | Why |
|----------|--------|-----|
| 1 | Delete 25 lane overrides | #1 source of routing bugs |
| 2 | CI checks for capability/family coverage | Prevents dark code |
| 3 | Instrument production | Real user data > synthetic prompts |
| 4 | Fine-tune embedding model on domain data | bge-small is general-purpose |
| 5 | Fat tarballs for top framework families | Biggest TTFP win ready to execute |
