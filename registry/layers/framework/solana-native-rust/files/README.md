# Solana Client Notes

- Keep the instruction surface narrow and explicit.
- Add PDA seeds and account layout constraints once the product flow is known.
- Pair this starter with `sdk:solana-web3` when the caller needs JavaScript transaction assembly.
