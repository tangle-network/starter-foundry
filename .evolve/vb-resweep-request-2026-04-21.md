# VB re-sweep request — validate starter-foundry R1+R2 interventions

**Requester:** starter-foundry (Drew)
**Date:** 2026-04-21
**Target repo:** blueprint-agent
**Estimated run cost/time:** fill in from prior sweeps (reference: gen21-sonnet-baseline)
**Response format:** reply in this doc under the §Results section, or link a gist

---

## Why you're getting this

starter-foundry shipped two capability-layer interventions today (R1 + R2). Both claim to reduce `scaffold_gap_installs` — the count of packages agents have to install themselves during a buildout because the composed scaffold arrived without them. The starter-foundry-side evidence is green (deterministic compose → install → typecheck) but the only authoritative validation is a VB sweep that exercises real agent buildouts across representative scenarios.

I need a sweep; you own VB. This doc tells you exactly what to run and what to send back.

## Pin these versions

```
starter-foundry  commit  c8f82dc  (branch feat/vb-feedback-consumer)
                 version 0.6.0  (symlink in blueprint-agent/node_modules
                                 already resolves here — no pnpm install needed)
blueprint-agent  whatever HEAD is on the branch you run gen32 from
```

Confirm both before kicking off. If blueprint-agent has drifted from the gen32 harness, use the gen32 variant config as-is — we're measuring starter-foundry impact, not blueprint-agent changes.

## What to run

**A full VB sweep across the scenario corpus**, not a smoke subset. We need signal on the specific families R1 + R2 touched (listed below). The gen32-sdk-live-local runs you started this morning (9 runs, all fintech-mixed) don't cover the R1/R2 target surfaces — please extend to a full corpus.

**Minimum scenario coverage we need:**

| Purpose | Partner | Scenarios (any that route here) |
|---|---|---|
| Validate R2 code-editor signals | `hyperliquid-perps` | `hl-builder-code-dashboard` (critical) |
| Validate R2 code-editor signals | `ethereum-l1` | `agent-trading` |
| Validate R1 tailwind cascade | `base-defi`, `solana-defi`, any UI-heavy vertical | anything that routes to `vision-first-agent`, `voice-first-agent`, `multimodal-agent`, `electron-native-os`, `realtime-audio-ts`, or `astro-static` |
| Validate R1 shadcn cascade | any UI-heavy vertical | anything that routes to `react-vite-ts`, `nextjs-ts`, `electron-native-os`, `multimodal-agent` |
| Regression check | baseline verticals | `fintech-mixed`, `ai-infra` — must not regress vs gen32-sdk-live-local baseline |

At minimum ~60–80 runs total. If the full corpus is bigger, run the full corpus.

**Run config (copy from gen32-sdk-live-local, adjust):**
- Backend: `claude-code` OAuth (Max subscription) — per gen32 meta.json
- `CI=true` (so TERMINAL_MAX_REQUESTS=100/s)
- `VB_SKIP_SEMANTIC=true` until the judge-model gateway has credits (per gen32)
- 3 shots budget per run
- rate-limit retry enabled (withRateLimitRetry — already in gen32)

Name the variant `gen33-r1r2-validate`. Seed from `gen32-sdk-live-local`.

## What we need back

Three artifacts, in order of priority:

### 1. Updated `buildout-analysis.json` (must-have)

The same schema as `starter-foundry/.evolve/buildout-analysis.json` currently generated by whatever script emits it. Specifically need:

- `topAddedPackages[]` — top ~25 with `{key, timesAdded, addedOnPass, addedOnFail}`. **Goal: `tailwindcss`, `@tailwindcss/vite`, `lucide-react`, `clsx`, `codemirror`, `@codemirror/view`, `@codemirror/state`, `@codemirror/lang-javascript` all drop significantly — ideally all drop to 0 on the families R1+R2 cover.**
- `perScenario[]` — at minimum `{partner, scenarioId, total, pass, meanScore}` so we can verify `hl-builder-code-dashboard` moves from 0/3 → passing.
- `topRewrittenFiles[]` — we want to see `src/App.tsx` and `index.html` rewrite counts.

Drop this at `blueprint-agent/<sweep-dir>/buildout-analysis.json` and send me the path.

### 2. Scorecard delta vs gen32 (should-have)

For each of these metrics, report `before (gen32-sdk-live-local-fintech-subset)` vs `after (gen33-r1r2-validate)`:

| metric | current | target | your reading |
|---|---|---|---|
| `scaffold_gap_installs` | 59 | ≤10 | __ |
| `buildout_pass_rate` | 0.818 | 0.85 | __ |
| `top_file_rewrite_count` | 13 | ≤5 | __ |
| `median_turns_per_buildout` | ? | ≤40 | __ |
| `median_wall_seconds_per_buildout` | ? | ≤600 | __ |

If the metric isn't computable from your sweep (e.g. different scenario coverage makes a direct comparison meaningless), say so — don't fabricate a number.

### 3. `hl-builder-code-dashboard` detail (nice-to-have)

This scenario is the R2 acceptance test. Prior state: 0/3 pass. Hypothesis: with the updated planner signals + `capability:code-editor` auto-attaching on "code dashboard" prompts, the scaffold now arrives with codemirror deps already declared, so the agent won't burn a shot adding them.

If it's still 0/3, we need a trace: which shot failed, with what error. If it's 3/3, we need the codemirror install count on each run — should be zero if things worked.

## Acceptance criteria

We'll call R1+R2 validated if, in the returned `buildout-analysis.json`:

1. **`tailwindcss` and `@tailwindcss/vite` timesAdded drop by ≥60%** (from 14 each → ≤6 each). Upper-bound claim was -14, -14; 60% is the honest ratchet accounting for families we didn't cover.
2. **At least one of `codemirror` / `@codemirror/view` / `@codemirror/state` / `@codemirror/lang-javascript` drops to 0 on `hl-builder-code-dashboard` specifically.**
3. **No regression on fintech-mixed** (pass rate within 1 scenario of gen32 baseline).
4. **No new package enters the top-10 `topAddedPackages` list** that wasn't there in gen32 (guards against cascading bugs from the intervention).

If 1+2 hit, R1+R2 ships and we move to next interventions (template regeneration, capability registry audit).
If 1 fails but 2 passes, R2 works but R1 signal attachments weren't exercised — we'd need a targeted UI-heavy vertical in the next sweep.
If 1+2 both fail with no regression, we roll forward but revisit diagnosis.
If 3 or 4 fails, we revert commits `77d4453` + `29d5ccc` immediately.

## What's explicitly NOT asked

- Don't regenerate the VB scenario corpus; use the existing one.
- Don't run blueprint-agent's internal self-evolution (gen33 should be a one-shot sweep, not a generation).
- Don't wait on anything blocked by the VB_SKIP_SEMANTIC=true flag — we're measuring gap_installs + pass_rate + file_rewrites, all of which are deterministic post-run analysis, no judge needed.

## Questions back to me

If any of these are true, reply before running:

- The gen32 harness itself is broken (variant_b/AxFlow regressed) — we'd need that fixed first.
- You don't have compute/credits for a full corpus run — we'll negotiate scope down.
- The starter-foundry symlink isn't pointing at c8f82dc — something about your env differs from mine; fix the symlink before running.

Otherwise just kick it off and ping me when it's done. I'll be watching `.evolve/vb-resweep-request-2026-04-21.md` for updates.

---

## §Results

_(leave this section for the VB operator to fill in)_

**Sweep name:** `gen33-r1r2-validate`
**Started:** __
**Completed:** __
**Total runs:** __
**Link to buildout-analysis.json:** __

### Metric deltas vs gen32

| metric | gen32 | gen33 | target | status |
|---|---|---|---|---|
| scaffold_gap_installs | __ | __ | ≤10 | __ |
| buildout_pass_rate | __ | __ | 0.85 | __ |
| top_file_rewrite_count | __ | __ | ≤5 | __ |

### Acceptance criteria check

- [ ] 1. tailwindcss + @tailwindcss/vite drop ≥60%
- [ ] 2. codemirror-cluster drops to 0 on hl-builder-code-dashboard
- [ ] 3. no regression on fintech-mixed
- [ ] 4. no new top-10 added package

### Notes / surprises / anything I should know

_(free-form)_
