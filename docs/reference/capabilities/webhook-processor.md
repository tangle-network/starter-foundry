# Capability: `capability:webhook-processor`

Webhook ingestion with HMAC signature verification, provider routing, and idempotent retry queue.

**Applies to**: api-service, agent-service-ts, agent-service-py
