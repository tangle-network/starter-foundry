# Family: `agent-service-py`

Python AI agent service starter with a small HTTP control plane.

**Taxonomy**: language=python · runtime=python · surface=agent-service

**Tags**: ai, agent, python, service

## Slots

- `database` — options: database:sqlite, database:postgres, database:mongodb, database:convex (default: `database:sqlite`)
- `sdk` — options: sdk:none, sdk:coinbase-cdp, sdk:solana-web3, sdk:evm-wallet (default: `sdk:none`)
- `auth` — options: auth:none, auth:better-auth, auth:clerk, auth:supabase-auth (default: `auth:none`)
- `payments` — options: payments:none, payments:stripe, payments:coinbase-commerce (default: `payments:none`)
- `queue` — options: queue:none, queue:bullmq, queue:trigger-dev (default: `queue:none`)

## Routing keywords

- **tier1**: python agent, pydanticai, crewai, autogen, agno, llamaindex, python chatbot, python assistant, unsloth
- **tier2**: agentkit, qlora
- **archetypes**: custom gpt, train a gpt, fine-tune gpt, document ocr, ocr pipeline, text extraction, pdf parser, document parser, image upscaler, super resolution, image enhancer, upscale image, sentiment analysis, sentiment analyzer, opinion mining
