# Capability: `capability:defi-yield`

Adds yield aggregator and vault strategy patterns with auto-compounding and reward harvesting.

**Applies to**: forge-contracts, hardhat-contracts, api-service, evm-infra-ts, worker-job
