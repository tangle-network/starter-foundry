{
  "id": "discord",
  "slot": "agent-channels",
  "description": "Discord channel adapter — interactions webhook (Ed25519-verified) + REST send. Composes with any agent-runtime bundle. Set DISCORD_BOT_TOKEN and DISCORD_PUBLIC_KEY at deploy. Discord requires every interaction POST to be verified against the application's public key; reject-loud on failure per Discord's security spec.",
  "appliesTo": [],
  "provides": ["channel:discord"],
  "requires": ["secure:webhook-in"],
  "defaults": {
    "channelDiscordAvailable": true
  },
  "files": [
    { "source": "files/channels/discord.ts", "target": "channels/discord.ts" },
    { "source": "files/channels/discord.env.example", "target": "channels/discord.env.example" }
  ],
  "validationChecks": [
    { "type": "file-exists", "path": "channels/discord.ts" }
  ],
  "contextHints": {
    "extensionPoints": ["channels/discord.ts"]
  }
}
